api_platform:
    title: 'PowerCloud API'
    description: 'Codility task for PowerCloud'
    version: '1.0.0'
    mapping:
        paths: ['%kernel.project_dir%/src/Entity']
    patch_formats:
        json: ['application/merge-patch+json']
    swagger:
        versions: [3]
    eager_loading:
        max_joins: 100
    defaults:
        pagination_items_per_page: 10
